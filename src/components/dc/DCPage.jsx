import React from 'react';
import { Typography } from '@material-ui/core';

export const DCPage = () => {
    return (
        <>
            <Typography variant="h1">DC</Typography>
        </>
    );
};
